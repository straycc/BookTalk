<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.talkserver.user.mapper.LikeRecordMapper">

    <delete id="deleteByUserAndTarget">
        DELETE FROM like_record
        WHERE user_id = #{userId}
          AND target_type  = #{likeTargetType}
          AND target_id = #{targetId}
    </delete>


    <select id="selectByUserTaeget" resultType="java.lang.Long">
        SELECT id FROM like_record
        WHERE user_id = #{userId}
          AND target_type  = #{targetType}
          AND target_id = #{targetId}
    </select>
</mapper>
