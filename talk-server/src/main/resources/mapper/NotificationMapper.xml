<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cc.talkserver.user.mapper.NotificationMapper">

    <!-- 批量标记为已读 -->
    <update id="batchMarkAsRead">
        UPDATE notification
        SET is_read = TRUE
        WHERE user_id = #{userId}
        AND id IN
        <foreach collection="notificationIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = FALSE
    </update>

    <!-- 批量删除通知 -->
    <update id="batchDelete">
        UPDATE notification
        SET is_deleted = TRUE
        WHERE user_id = #{userId}
        AND id IN
        <foreach collection="notificationIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = FALSE
    </update>

    <!-- 清空已读通知 -->
    <update id="clearReadNotifications">
        UPDATE notification
        SET is_deleted = TRUE
        WHERE user_id = #{userId}
        AND is_read = TRUE
        AND is_deleted = FALSE
    </update>

</mapper>